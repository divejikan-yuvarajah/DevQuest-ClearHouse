import type { Knex } from "knex";

// Demo data: every account should already hold an initial amount when the dashboard loads.
// Not implemented yet. See tests/challenge20.test.ts (20a) for what the seed must create.
export async function seed(_knex: Knex): Promise<void> {}
